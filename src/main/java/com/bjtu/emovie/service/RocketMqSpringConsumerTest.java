package com.bjtu.emovie.service;
/*
import com.bjtu.emovie.job.ExampleData;
import org.apache.rocketmq.spring.annotation.RocketMQMessageListener;
import org.apache.rocketmq.spring.core.RocketMQListener;
import org.springframework.stereotype.Service;

@Service
@RocketMQMessageListener(nameServer = "${rocketmq.name-server}", topic = "SpringTopicTest", consumerGroup = "consumerGroup01")
public class RocketMqSpringConsumerTest implements RocketMQListener<ExampleData> {
    @Override
    public void onMessage(ExampleData message) {
        System.out.printf("######## user_consumer received: %s ; num: %s ; str: %s \n", message, message.getNum(), message.getStr());
    }
}
*/
public class RocketMqSpringConsumerTest{

}